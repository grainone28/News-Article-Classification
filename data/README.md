# Data Directory

This folder contains the dataset files used for the project:

- `development.csv`: labeled dataset used for training and validation
- `evaluation.csv`: unlabeled dataset used for final prediction

The dataset was provided by the course instructor and is not publicly distributable.

To run the project locally, place both CSV files inside this folder.